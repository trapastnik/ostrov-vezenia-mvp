upstream backend {
    server backend:8000;
}

# API + Admin — единый server block
# Traefik маршрутизирует по домену, nginx обрабатывает внутренние пути
server {
    listen 80;
    server_name _;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' https://api.ostrov-vezeniya.ru; frame-src 'self' blob:; object-src blob:; frame-ancestors 'self'" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # Health check
    location /health {
        proxy_pass http://backend;
    }

    # API
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 30s;
        client_max_body_size 10m;
    }

    # Swagger docs — только с localhost
    location /docs {
        allow 127.0.0.1;
        deny all;
        proxy_pass http://backend;
    }

    location /openapi.json {
        allow 127.0.0.1;
        deny all;
        proxy_pass http://backend;
    }

    # Shop emulator
    location /shop {
        proxy_pass http://backend;
    }

    # Admin SPA (fallback)
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
